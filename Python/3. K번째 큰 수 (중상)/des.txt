[풀이 팁]
카드를 임의로 3장 뽑아서 합을 구한다.
-> 중복된 값이 나올 수 없다.
-> i, j, k를 i, i+1, j+1과 같이 물린다.

[새로 알게된 정보]
set 자료형의 중요성을 깨달았다. (코테에서 매우 유용할 듯)
- 중복 자동 제거
- var=set()으로 초기화 가능

dict 초기화
- var={}
- var={1:2}

set 초기화
- var={1}